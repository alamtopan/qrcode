.box
  .box-header
    %b
      = params[:action]
      = link_to 'Back To List', backend_students_path, class: 'btn btn-info btn-xs pull-right'
    %hr
  .box-body.table-responsive
    = form_for @student, url: backend_student_path, method: 'put' do |f|
      = render 'form', {f: f}
